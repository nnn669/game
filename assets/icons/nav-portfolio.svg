<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 32 32" fill="none">
  <defs>
    <linearGradient id="pGrad" x1="0" y1="0" x2="1" y2="1">
      <stop offset="0%" stop-color="#60a5fa"/>
      <stop offset="100%" stop-color="#2563eb"/>
    </linearGradient>
  </defs>
  <!-- Pie chart segments -->
  <!-- Large segment ~60% (blue) -->
  <path d="M16 16 L16 4 A12 12 0 1 1 4.7 22.5 Z" fill="url(#pGrad)" opacity="0.9"/>
  <!-- Medium segment ~25% (teal) -->
  <path d="M16 16 L4.7 22.5 A12 12 0 0 1 10.2 5.6 Z" fill="#10b981" opacity="0.85"/>
  <!-- Small segment ~15% (amber) -->
  <path d="M16 16 L10.2 5.6 A12 12 0 0 1 16 4 Z" fill="#f59e0b" opacity="0.85"/>
  <!-- Center hole (donut) -->
  <circle cx="16" cy="16" r="6" fill="#0f172a"/>
  <!-- Center dot -->
  <circle cx="16" cy="16" r="2" fill="#334155"/>
  <!-- Outer ring -->
  <circle cx="16" cy="16" r="12" fill="none" stroke="#1e293b" stroke-width="0.5"/>
</svg>
